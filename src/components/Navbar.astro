---
import { withBase } from "../utils";
import { siteConfig } from "../config/site.config";
import { navConfig } from "../config/nav.config";
import Button from "./Button.astro";

const currentPath = Astro.url.pathname;
---
<header class="navbar fixed w-full top-0 z-50 py-4">
  <div class="container mx-auto px-4 md:px-6">
    <div class="flex items-center justify-between">
      <!-- Logo -->
      <a href={withBase("/")} class="flex items-center gap-2 group flex-shrink-0">
        <div class="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center group-hover:bg-blue-500 transition-colors flex-shrink-0">
          <span class="text-white font-bold font-display text-xl">N</span>
        </div>
        <span class="text-lg sm:text-xl font-bold font-display text-white tracking-tight truncate max-w-[150px] min-[400px]:max-w-none">{siteConfig.name.split(' ')[0]}<span class="text-blue-500">IT</span></span>
      </a>

      <!-- Desktop Nav -->
      <nav class="hidden md:flex items-center gap-8">
        {navConfig.links.map(link => (
          <a
            href={withBase(link.href)}
            class={`text-sm font-medium transition-colors hover:text-blue-400 ${currentPath === link.href || (currentPath.startsWith(link.href) && link.href !== '/') ? 'text-blue-500' : 'text-gray-300'}`}
          >
            {link.label}
          </a>
        ))}
      </nav>

      <!-- CTA & Mobile Toggle -->
      <div class="flex items-center gap-4">
        <div class="hidden md:block">
          <Button label={navConfig.cta.label} href={withBase(navConfig.cta.href)} variant="primary" />
        </div>
        <button id="mobile-menu-btn" class="md:hidden text-gray-300 hover:text-white focus:outline-none p-2 -mr-2" aria-label="Toggle menu">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="md:hidden absolute top-full left-0 w-full bg-[#0A0F1E] border-t border-gray-800 hidden flex-col max-h-[calc(100vh-5rem)] overflow-y-auto">
    {navConfig.links.map(link => (
      <a
        href={withBase(link.href)}
        class="px-6 py-4 border-b border-gray-800 text-gray-300 hover:bg-gray-800/50 hover:text-white"
      >
        {link.label}
      </a>
    ))}
    <div class="p-6">
      <Button label={navConfig.cta.label} href={withBase(navConfig.cta.href)} variant="primary" class="w-full" />
    </div>
  </div>
</header>

<script>
  const setupNav = () => {
    const navbar = document.querySelector('.navbar');
    const mobileBtn = document.getElementById('mobile-menu-btn');
    const mobileMenu = document.getElementById('mobile-menu');

    // Scroll effect
    window.addEventListener('scroll', () => {
      if (window.scrollY > 50) {
        navbar?.classList.add('scrolled');
      } else {
        navbar?.classList.remove('scrolled');
      }
    });

    // Initial check
    if (window.scrollY > 50) {
      navbar?.classList.add('scrolled');
    }

    // Mobile menu toggle
    mobileBtn?.addEventListener('click', () => {
      mobileMenu?.classList.toggle('hidden');
      mobileMenu?.classList.toggle('flex');
    });
  };

  document.addEventListener('DOMContentLoaded', setupNav);
</script>
